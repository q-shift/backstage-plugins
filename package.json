{
  "name": "root",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "18 || 20"
  },
  "scripts": {
    "build": "turbo run build",
    "clean": "turbo run clean",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "tsc": "turbo run tsc"
  },
  "devDependencies": {
    "@backstage/cli": "^0.25.2",
    "turbo": "^1.10.4",
    "typescript": "~5.2.0"
  },
  "workspaces": {
    "packages": [
      "plugins/*"
    ]
  }
}
